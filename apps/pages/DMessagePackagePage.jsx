import React from 'react';
import Layout from '../src/components/layout/Layout';
import DMessage from '../src/components/dMessage/dMessage';

const DMessagePackagePage = () => (
  <Layout>
    <DMessage />
  </Layout>
);

export default DMessagePackagePage;
